s = "paper";
t = "title";

var isIsomorphic = function (s, t) {

    // Initiate new arrays
    arrS = [];
    arrT = [];

    // Push string letters into arrays
    for (i = 0; i < s.length; i++) {
        arrS.push(s[i]);
    }

    for (i = 0; i < t.length; i++) {
        arrT.push(t[i]);
    }

    console.log(arrS);
    console.log(arrT);


    for (i = 0; i < s.length; i++) {
        if () {


            return true;
        }
        return false;
    }

};

isIsomorphic(s, t);